targets:

  cli-lunar:
    vars:
      BUILD_MINIMAL: "no"
      BUILD_DESKTOP: "no"
      RELEASE: lunar

    items-from-inventory:
      not-eos: yes

  cli-jammy:
    vars:
      BUILD_MINIMAL: "no"
      BUILD_DESKTOP: "no"
      RELEASE: jammy

    items-from-inventory:
      not-eos: yes

  cli-bookworm:
    vars:
      BUILD_MINIMAL: "no"
      BUILD_DESKTOP: "no"
      RELEASE: bookworm

    items-from-inventory:
      not-eos: yes

  cli-bullseye:
    vars:
      BUILD_MINIMAL: "no"
      BUILD_DESKTOP: "no"
      RELEASE: boolseye

    items-from-inventory:
      not-eos: yes

  minimal-lunar:
    vars:
      BUILD_MINIMAL: "yes"
      BUILD_DESKTOP: "no"
      RELEASE: lunar

    items-from-inventory:
      not-eos: yes

  minimal-jammy:
    vars:
      BUILD_MINIMAL: "yes"
      BUILD_DESKTOP: "no"
      RELEASE: jammy

    items-from-inventory:
      not-eos: yes

  minimal-bookworm:
    vars:
      BUILD_MINIMAL: "yes"
      BUILD_DESKTOP: "no"
      RELEASE: bookworm

    items-from-inventory:
      not-eos: yes

  minimal-bullseye:
    vars:
      BUILD_MINIMAL: "yes"
      BUILD_DESKTOP: "no"
      RELEASE: boolseye

    items-from-inventory:
      not-eos: yes
